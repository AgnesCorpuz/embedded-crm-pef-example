<div id="myBody" style="padding-left: 35px">
  <div class="w3-twothird w3-container">
    <h1 class="w3-text-deep-orange">Call Logs</h1>
    <table>
        <thead>
            <th>Interaction ID</th>
            <th>ANI</th>
            <th>Notes</th>
            <th>Attributes</th>
            <th>Association</th>
            <th>Association ID</th>
        </thead>
        <tbody>                
            {{#each callLogs as |log|}}
                <tr>
                    <td>{{log.data.interactionId.id}}</td>
                    <td>{{log.data.interactionId.ani}}</td>
                    <td>{{log.data.callLog.notes}}</td>
                    <td>{{log.data.callLog.attributes.pef_exampleworkspacekey}}</td>
                    <td>{{log.data.callLog.selectedContact.text}}</td>
                    <td>{{log.data.callLog.selectedContact.id}}</td>
                </tr>                        
            {{/each}}
        </tbody>
    </table>
  </div>
</div>

{{yield}}